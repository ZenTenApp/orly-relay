// Package common/jsbridge/subtle
// Generated by TinyJS — TinyGo JavaScript backend

import * as $rt from './$runtime/index.mjs';

// Package-level variables

export function init() {
  return;
}

// jsbridge: subtle.RandomBytes
export function RandomBytes(dst) {
  $rt.subtle.RandomBytes(dst);
}

// jsbridge: subtle.AESCBCEncrypt
export function AESCBCEncrypt(key, iv, plaintext, fn) {
  $rt.subtle.AESCBCEncrypt(key, iv, plaintext, fn);
}

// jsbridge: subtle.AESCBCDecrypt
export function AESCBCDecrypt(key, iv, ciphertext, fn) {
  $rt.subtle.AESCBCDecrypt(key, iv, ciphertext, fn);
}

